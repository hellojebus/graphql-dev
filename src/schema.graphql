type Query {
    info: String!
    people: [Person!]!
    person(id: ID!): Person
}

type Mutation {
    addPerson(name: String!): Person!
    updatePerson(id: ID!, name: String!): Person
    deletePerson(id: ID!): [Person]
}

type Person {
    id: ID!
    name: String!
}